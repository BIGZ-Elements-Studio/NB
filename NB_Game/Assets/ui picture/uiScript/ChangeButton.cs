
using UnityEngine;
using UnityEngine.UI;

public class ChangeButton : MonoBehaviour
{
    // Start is called before the first frame update
    [SerializeField] public Button FirstButton, SecondButton, ThirdButton;
   
}
